{
  "name": "@3n3a/home",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "wdev": "wrangler pages dev --local -b WEATHER_API_KEY=$(grep -E '^WEATHER_API_KEY=' .env | cut -d '=' -f 2- | sed 's/^\"\\(.*\\)\"$/\\1/') -- npm run dev"
  },
  "devDependencies": {
    "@astrojs/prefetch": "^0.2.1",
    "@astrojs/sitemap": "1.3.3",
    "astro": "2.6.4",
    "astro-seo": "0.7.4"
  }
}
